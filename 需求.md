# 先锋网络中心2026招新报名系统需求文档

## 1. 项目概述
本项目为先锋网络中心2026年招新报名系统，提供在线问卷填写、修改、提交功能。用户通过QQ号和学号登录，填写报名问卷（含部门志愿拖拽排序），提交后数据保存至SQLite数据库。系统需支持登录日志、修改日志记录，并具备防攻击、防重复提交等安全机制。

## 2. 技术架构
- **后端**：Python
- **前端**：HTML/CSS/JavaScript，响应式设计，适配手机和电脑
- **数据库**：SQLite
- **数据交互**：JSON格式为主，特殊字段（个人优势及未来工作设想）前端Base64编码后提交
- **设计风格**：前端UI采用 awesome-design-md 的 x.ai 设计风格，可参考 huashu-design 进行视觉补充（如东花等元素）

## 3. 功能需求

### 3.1 登录与身份验证

#### 3.1.1 登录流程
- 用户打开系统时默认重定向到登录页面。
- 登录页面输入 **QQ号** 和 **学号**，点击登录后由后端校验。
- 登录成功后，后端返回 **姓名**，前端获取姓名、学号、QQ号，均不可修改。
- 登录状态不保持（无持久化会话，关闭浏览器或超时后需重新登录）。

#### 3.1.2 登录名单来源
- 程序实时读取固定路径的CSV文件（不锁定文件，避免影响外部修改）。
- CSV文件列顺序为：`QQ号, 学号, 姓名`（无表头，或表头可配置，由开发者决定）。
- 文件更新由其他程序负责，本系统仅读取。

#### 3.1.3 校验规则
- 仅在CSV中存在的用户可登录。
- 异常情况与提示：
  - QQ号存在但学号为空：提示 **“请群内实名后继续。”**
  - QQ号不存在：提示 **“请加群后继续。”**
  - 学号与CSV中学号不完全匹配：提示 **“学号与 QQ 号不匹配，请核对后重试。”**

#### 3.1.4 登录日志
- 记录所有登录操作（成功/失败），字段由开发者设计，需包含用户标识、时间、IP、User-Agent、结果等，便于追溯。

### 3.2 问卷填写

#### 3.2.1 问卷标题
固定为：**先锋网络中心2026招新报名表**

#### 3.2.2 字段列表
| 字段 | 类型 | 说明 |
|------|------|------|
| 姓名 | 文本（只读） | 来自登录信息，不可修改 |
| 学号 | 文本（只读） | 来自登录信息，不可修改 |
| QQ号 | 文本（只读） | 来自登录信息，不可修改 |
| 性别 | 单选题 | 选项：男 / 女，必填 |
| 想要报名的部门 | 自定义拖拽排序题 | 详见3.2.3 |
| 是否服从部门间调剂 | 单选题 | 选项：是 / 否，必填 |
| 个人优势及未来工作设想 | 多行文本 | 必填，提交时Base64编码 |

#### 3.2.3 部门拖拽排序题设计
- **选项**（共5个，固定）：
  1. 设备服务站 - 硬件部
  2. 网络技术站 - 网络部
  3. 基础事务站 - 办公室
  4. 基础事务站 - 美工部
  5. 基础事务站 - 推广部
- **电脑端UI**：
  - 左侧为5个可拖动方块（初始状态全部位于左侧）。
  - 右侧为容器，接收左侧拖入的方块，容器内从上到下具有顺序。
- **交互规则**：
  - 拖入容器即视为报名该部门，容器内顺序代表志愿优先级。
  - 容器内方块可上下拖动调整顺序。
  - 容器内方块可拖回左侧（取消报名）。
  - 至少选择1个部门，至多选择5个。
  - 初始状态：左侧5个方块，右侧容器为空。
- **手机端**：交互方式不做特定要求，保证功能可用即可（如点击添加、上下移动按钮等）。

### 3.3 提交与确认

#### 3.3.1 提交前确认弹窗
- 用户点击“提交”按钮时，弹出确认提示框，展示：
  - 姓名
  - 想要报名的部门（按容器内顺序以列表展示，编号）
  - 是否接受调剂
  - 个人优势及未来工作设想（内容较长时可滚动）
- 提供“确认”和“取消”按钮：
  - 取消：关闭弹窗，返回问卷修改。
  - 确认：提交数据，后端保存，跳转至`success.html`。

#### 3.3.2 提交成功页
- 跳转至静态页面`success.html`（内容由开发者设计，无需用户提供）。

#### 3.3.3 防重复提交
- 前端：提交后禁用按钮，防止快速重复点击。
- 后端：进行幂等性校验，拒绝短时间内重复提交相同内容（具体机制由开发者设计，如令牌、请求ID等）。

### 3.4 修改与数据加载
- 用户再次登录时，自动加载上一次提交的问卷数据（若存在），包括所有字段和部门顺序。
- 不提供草稿功能，仅保存最后一次提交的结果。
- 不限修改次数，每次修改均记录修改日志。

### 3.5 截止时间处理
- 最晚提交时间硬编码在Python主代码开头，定义为常量，便于修改。
- 设定值：**2026年9月29日 12:00:00**
- 报名无开始时间。
- 超过截止时间后：
  - 前端：用户进入问卷时显示`timeend.html`页面。
  - 后端：拒绝一切登录、查看、修改、提交请求。

## 4. 数据库需求
- 数据库文件存储于固定路径（由开发者指定）。
- 表结构由开发者自行设计，需满足以下存储需求：
  - **用户问卷结果**：仅保存最后一次提交的完整数据（覆盖式），字段包括姓名、学号、QQ号、性别、部门顺序、是否调剂、个人优势等。
  - **登录日志**：记录每次登录尝试，包括成功和失败。
  - **修改日志**：增量记录每次修改操作，需能还原修改历史（如字段原值→新值、修改时间、操作人等）。
- 部门顺序存储格式建议为有序字符串（如JSON数组）或关联表，由开发者决定。

## 5. 安全需求
- **输入安全**：防止SQL注入、XSS等，使用参数化查询、输入校验。
- **Base64编码**：仅对“个人优势及未来工作设想”字段进行Base64编码传输，后端解码后进行长度限制和内容过滤。
- **身份校验与防攻击**：提交时后端需进行严格身份校验，防止重放攻击、恶意提交无意义记录等。措施可包括：
  - 登录后发放一次性token，提交时校验token有效性。
  - 限制请求频率（如IP限流、用户级限流）。
  - 对异常高频提交进行拦截。
  - 引入时间戳或随机数防止重放。
  - 具体方案由开发者设计，确保系统稳健。

## 6. 前端设计风格
- 遵循 awesome-design-md 的 x.ai 设计风格。
- 可参考 huashu-design 进行视觉补充（如东花等元素）。
- 响应式设计，保证在手机和电脑上均有良好体验。

## 7. 其他说明
- CSV文件仅由本系统读取，不负责写入更新。
- 静态页面`success.html`和`timeend.html`由开发者自行设计，本项目不涉及具体内容。
- 未特别指定的细节（如日志字段、表结构、限流策略等）均由开发者根据经验合理实现。

---

**文档版本**：1.0  
**最后更新**：2026-08-29